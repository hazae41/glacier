import { Handle } from "../handles";
/**
 * Show handle in console when it changes
 * @param handle
 */
export declare function useDebug<D extends N = any, E = any, N = D, K = any>(handle: Handle<D, E>, label: string): void;
